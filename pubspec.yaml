name: calculator_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
hive: ^2.2.3
hive_flutter: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
hive_generator: ^1.2.0
build_runner: ^2.4.6

flutter:
  uses-material-design: true
